---
// Contact.astro
---

<!-- Línea divisora de colores -->
<div class="w-full h-1 bg-gradient-to-r from-indigo-500 via-purple-500 via-green-500 via-blue-500 to-indigo-500 animate-pulse"></div>

<section class="min-h-screen flex items-center justify-center p-6 bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-100 dark:from-slate-900 dark:via-slate-800 dark:to-indigo-950">
    <div class="max-w-7xl w-full grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
        
        <!-- Columna Izquierda - Contenido Visual -->
        <div class="order-2 lg:order-1 flex flex-col justify-center items-center lg:items-start text-center lg:text-left space-y-8">
            
            <!-- Hero Text -->
            <div class="space-y-6">
                <h1 class="text-4xl lg:text-6xl font-bold bg-gradient-to-r from-indigo-600 via-purple-600 to-blue-600 dark:from-indigo-400 dark:via-purple-400 dark:to-blue-400 bg-clip-text text-transparent animate-pulse-slow">
                    ¡Hablemos!
                </h1>
                <p class="text-lg lg:text-xl text-gray-600 dark:text-gray-300 max-w-lg">
                    ¿Tienes una idea increíble? ¿Un proyecto en mente? ¿O simplemente quieres conectar? 
                    <span class="text-indigo-600 dark:text-indigo-400 font-semibold">¡Me encantaría escucharte!</span>
                </p>
            </div>

            <!-- Tarjetas de Información -->
            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 w-full max-w-lg">
                <div class="bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-2xl p-6 shadow-lg border border-white/20 dark:border-slate-700/50 hover:shadow-xl transition-all duration-300 animate-float">
                    <div class="text-3xl mb-3">⚡</div>
                    <h3 class="font-semibold text-gray-800 dark:text-white mb-2">Respuesta Rápida</h3>
                    <p class="text-sm text-gray-600 dark:text-gray-300">Te responderé en menos de 24 horas</p>
                </div>
                
                <div class="bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-2xl p-6 shadow-lg border border-white/20 dark:border-slate-700/50 hover:shadow-xl transition-all duration-300 animate-float" style="animation-delay: 1s;">
                    <div class="text-3xl mb-3">🚀</div>
                    <h3 class="font-semibold text-gray-800 dark:text-white mb-2">Proyectos Frontend</h3>
                    <p class="text-sm text-gray-600 dark:text-gray-300">Especializado en Astro y TailwindCSS</p>
                </div>
                
                <div class="bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-2xl p-6 shadow-lg border border-white/20 dark:border-slate-700/50 hover:shadow-xl transition-all duration-300 animate-float" style="animation-delay: 2s;">
                    <div class="text-3xl mb-3">🎨</div>
                    <h3 class="font-semibold text-gray-800 dark:text-white mb-2">Diseño UI/UX</h3>
                    <p class="text-sm text-gray-600 dark:text-gray-300">Experiencias digitales modernas</p>
                </div>
                
                <div class="bg-white/80 dark:bg-slate-800/80 backdrop-blur-sm rounded-2xl p-6 shadow-lg border border-white/20 dark:border-slate-700/50 hover:shadow-xl transition-all duration-300 animate-float" style="animation-delay: 3s;">
                    <div class="text-3xl mb-3">💡</div>
                    <h3 class="font-semibold text-gray-800 dark:text-white mb-2">Ideas Creativas</h3>
                    <p class="text-sm text-gray-600 dark:text-gray-300">Soluciones innovadoras y únicas</p>
                </div>
            </div>

            <!-- Elementos decorativos -->
            <div class="hidden lg:block absolute top-20 left-20 w-64 h-64 bg-gradient-to-r from-indigo-400/20 to-purple-400/20 rounded-full blur-3xl animate-pulse-slow"></div>
            <div class="hidden lg:block absolute bottom-20 left-10 w-48 h-48 bg-gradient-to-r from-blue-400/20 to-indigo-400/20 rounded-full blur-2xl animate-pulse-slow" style="animation-delay: 2s;"></div>
        </div>

        <!-- Columna Derecha - Formulario -->
        <div class="order-1 lg:order-2 flex justify-center">
            <div class="w-full max-w-md">
                <form class="bg-white/90 dark:bg-slate-800/90 backdrop-blur-xl rounded-3xl shadow-2xl border border-white/30 dark:border-slate-700/50 p-8 space-y-6 animate-glow" id="contactForm">
                    
                    <!-- Header del formulario -->
                    <div class="text-center space-y-2">
                        <h2 class="text-2xl font-bold text-gray-800 dark:text-white">Contáctame</h2>
                        <p class="text-gray-600 dark:text-gray-300">Cuéntame sobre tu proyecto</p>
                    </div>

                    <!-- Campos del formulario -->
                    <div class="space-y-4">
                        <div class="group">
                            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">¿Cómo te llamas?</label>
                            <input
                                type="text"
                                placeholder="Dime tu nombre"
                                name="name"
                                required
                                class="w-full px-4 py-4 rounded-xl border-2 border-gray-200 dark:border-gray-600 bg-white/50 dark:bg-slate-900/50 text-gray-800 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-500/20 transition-all duration-300 group-hover:border-indigo-300"
                            />
                        </div>

                        <div class="group">
                            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Tu email</label>
                            <input
                                type="email"
                                name="_replyto"
                                placeholder="sapitobonito@gmail.com"
                                required
                                class="w-full px-4 py-4 rounded-xl border-2 border-gray-200 dark:border-gray-600 bg-white/50 dark:bg-slate-900/50 text-gray-800 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-500/20 transition-all duration-300 group-hover:border-indigo-300"
                            />
                        </div>

                        <div class="group">
                            <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Cuéntame tu idea</label>
                            <textarea
                                rows="4"
                                placeholder="Describe tu proyecto, idea o simplemente salúdame"
                                name="message"
                                required
                                class="w-full px-4 py-4 rounded-xl border-2 border-gray-200 dark:border-gray-600 bg-white/50 dark:bg-slate-900/50 text-gray-800 dark:text-white placeholder-gray-400 dark:placeholder-gray-500 focus:outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-500/20 transition-all duration-300 resize-none group-hover:border-indigo-300"
                            ></textarea>
                        </div>
                    </div>

                    <!-- Botón de envío -->
                    <button
                        type="submit"
                        class="w-full bg-gradient-to-r from-indigo-500 via-purple-500 to-blue-500 hover:from-indigo-600 hover:via-purple-600 hover:to-blue-600 text-white font-semibold py-4 px-6 rounded-xl transition-all duration-300 transform hover:scale-[1.02] hover:shadow-xl focus:outline-none focus:ring-4 focus:ring-indigo-500/50 active:scale-[0.98]"
                    >
                        <span class="flex items-center justify-center space-x-2">
                            <span>Enviar mensaje</span>
                            <span class="text-lg"></span>
                        </span>
                    </button>
                </form>
            </div>
        </div>
    </div>
</section>

<!-- Modal de éxito -->
<div id="modal" class="fixed top-6 right-6 bg-gradient-to-r from-green-500 to-emerald-500 text-white px-6 py-4 rounded-2xl shadow-2xl opacity-0 pointer-events-none transition-all duration-500 transform translate-x-full border border-green-400/30 z-50">
    <div class="flex items-center space-x-3">
        <div class="text-2xl animate-bounce">✅</div>
        <div>
            <div class="font-semibold">¡Mensaje enviado!</div>
            <div class="text-sm opacity-90">Te responderé muy pronto</div>
        </div>
    </div>
</div>

<!-- Modal de recomendación modo oscuro -->
<div id="darkModeModal" class="fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center p-4 opacity-0 pointer-events-none transition-all duration-500 z-50">
    <div class="bg-gradient-to-br from-indigo-600 via-purple-600 to-blue-600 text-white p-8 rounded-3xl shadow-2xl max-w-md w-full border border-white/20 transform scale-90 transition-all duration-300">
        <div class="text-center space-y-4">
            <div class="text-6xl animate-pulse-slow"></div>
            <h3 class="text-2xl font-bold">Recomendación</h3>
            <p class="text-lg opacity-90">
                Para una mejor experiencia visual, te recomendamos usar 
                <span class="font-semibold text-yellow-300">modo oscuro</span>
            </p>
            <div class="flex flex-col sm:flex-row gap-3 pt-4">
                <button id="enableDarkMode" class="flex-1 bg-white/20 hover:bg-white/30 text-white font-semibold py-3 px-6 rounded-xl transition-all duration-300 transform hover:scale-105 border border-white/30">
                    Activar modo oscuro 
                </button>
                <button id="closeDarkModeModal" class="flex-1 bg-white/10 hover:bg-white/20 text-white font-semibold py-3 px-6 rounded-xl transition-all duration-300 border border-white/20">
                    Continuar por defecto (claro)
                </button>
            </div>
            <p class="text-xs opacity-70 pt-2">
                (Este mensaje solo aparece una vez)
            </p>
        </div>
    </div>
</div>

<style>
    @keyframes float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-20px); }
    }
    
    @keyframes pulse-slow {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.5; }
    }
    
    @keyframes glow {
        from { box-shadow: 0 0 20px rgba(99, 102, 241, 0.3); }
        to { box-shadow: 0 0 40px rgba(99, 102, 241, 0.6); }
    }
    
    .animate-float {
        animation: float 12s ease-in-out infinite;
    }
    
    .animate-pulse-slow {
        animation: pulse-slow 4s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }
    
    .animate-glow {
        animation: glow 2s ease-in-out infinite alternate;
    }
</style>

<script>
    const form = document.getElementById("contactForm") as HTMLFormElement;
    const modal = document.getElementById("modal") as HTMLElement;
    const darkModeModal = document.getElementById("darkModeModal") as HTMLElement;
    const enableDarkModeBtn = document.getElementById("enableDarkMode") as HTMLButtonElement;
    const closeDarkModeModalBtn = document.getElementById("closeDarkModeModal") as HTMLButtonElement;

    // Función para mostrar el modal de modo oscuro
    function showDarkModeModal() {
        // Solo mostrar si no se ha mostrado antes
        const hasSeenModal = localStorage.getItem('darkModeModalSeen');
        
        if (!hasSeenModal) {
            setTimeout(() => {
                darkModeModal?.classList.remove("opacity-0", "pointer-events-none");
                darkModeModal?.querySelector("div")?.classList.remove("scale-90");
                darkModeModal?.querySelector("div")?.classList.add("scale-100");
            }, 1000); // Mostrar después de 1 segundo
        }
    }

    // Función para activar modo oscuro
    function enableDarkMode() {
        document.documentElement.classList.add('dark');
        localStorage.setItem('theme', 'dark');
        closeDarkModeModal();
    }

    // Función para cerrar el modal
    function closeDarkModeModal() {
        darkModeModal?.classList.add("opacity-0", "pointer-events-none");
        darkModeModal?.querySelector("div")?.classList.add("scale-90");
        darkModeModal?.querySelector("div")?.classList.remove("scale-100");
        localStorage.setItem('darkModeModalSeen', 'true');
    }

    // Event listeners para el modal de modo oscuro
    enableDarkModeBtn?.addEventListener("click", enableDarkMode);
    closeDarkModeModalBtn?.addEventListener("click", closeDarkModeModal);

    // Cerrar modal al hacer click en el fondo
    darkModeModal?.addEventListener("click", (e) => {
        if (e.target === darkModeModal) {
            closeDarkModeModal();
        }
    });

    // Detectar tema del sistema y mostrar modal
    window.addEventListener('DOMContentLoaded', () => {
        // Aplicar tema guardado o detectar preferencia del sistema
        const savedTheme = localStorage.getItem('theme');
        const systemPrefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        
        if (savedTheme === 'dark' || (!savedTheme && systemPrefersDark)) {
            document.documentElement.classList.add('dark');
        }

        // Mostrar modal de recomendación
        showDarkModeModal();
    });

    // Formulario de contacto
    form?.addEventListener("submit", async (e) => {
        e.preventDefault();

        const data = new FormData(form);
        const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement;
        
        // Cambiar estado del botón durante el envío
        if (submitBtn) {
            submitBtn.innerHTML = `
                <span class="flex items-center justify-center space-x-2">
                    <span>Enviando...</span>
                    <div class="animate-spin h-5 w-5 border-2 border-white/30 border-t-white rounded-full"></div>
                </span>
            `;
            submitBtn.disabled = true;
        }

        try {
            const response = await fetch("https://formspree.io/f/meokvwvd", {
                method: "POST",
                body: data,
                headers: {
                    Accept: "application/json",
                },
            });

            if (response.ok) {
                // Mostrar modal de éxito
                modal?.classList.remove("opacity-0", "pointer-events-none", "translate-x-full");
                
                setTimeout(() => {
                    modal?.classList.add("opacity-0", "pointer-events-none", "translate-x-full");
                    form.reset();
                }, 5000);
            } else {
                throw new Error("Error en el servidor");
            }
        } catch (err) {
            alert("Error al enviar el mensaje: " + (err as Error).message);
        } finally {
            // Restaurar botón
            if (submitBtn) {
                submitBtn.innerHTML = `
                    <span class="flex items-center justify-center space-x-2">
                        <span>Enviar mensaje</span>
                        <span class="text-lg">📨</span>
                    </span>
                `;
                submitBtn.disabled = false;
            }
        }
    });

    // Efecto de partículas en el fondo
    function createParticle() {
        const particle = document.createElement('div');
        particle.className = 'fixed pointer-events-none w-2 h-2 bg-indigo-400/20 rounded-full animate-pulse-slow';
        particle.style.left = Math.random() * 100 + '%';
        particle.style.top = Math.random() * 100 + '%';
        particle.style.animationDelay = Math.random() * 4 + 's';
        document.body.appendChild(particle);
        
        setTimeout(() => {
            particle.remove();
        }, 8000);
    }

    // Crear partículas cada 3 segundos
    setInterval(createParticle, 3000);
</script>